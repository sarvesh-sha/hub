/**
 * Copyright (C) 2017-, Optio3, Inc. All Rights Reserved.
 *
 * Proprietary & Confidential Information.
 *
 * Optio3 Builder APIs
 * APIs and Definitions for the Optio3 Builder product.
 *
 * OpenAPI spec version: 1.0.0
 *
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

import * as models from './models';

export class DeploymentTask {
    static readonly RECORD_IDENTITY: string = "DeploymentTask";

    getFixupPrototypeFunction() { return DeploymentTask.fixupPrototype; }

    static newInstance(model: Partial<DeploymentTask>): DeploymentTask {
        let obj = Object.assign(new DeploymentTask(), model);
        DeploymentTask.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<DeploymentTask>): DeploymentTask {
        if (!model) return null;
        return DeploymentTask.newInstance(<DeploymentTask> JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: DeploymentTask) {
        if (!obj) return;

        Object.setPrototypeOf(obj, models.DeploymentTask.prototype);

        obj.fixupFields();
    }

    fixupFields() {
        if (this.restartCount === undefined) {
            this.restartCount = 0;
        }
        if (this.lastOffset === undefined) {
            this.lastOffset = 0;
        }
        if (typeof this.createdOn === "string") {
            this.createdOn = new Date(<string><any>this.createdOn);
        }
        if (typeof this.updatedOn === "string") {
            this.updatedOn = new Date(<string><any>this.updatedOn);
        }
        if (typeof this.lastHeartbeat === "string") {
            this.lastHeartbeat = new Date(<string><any>this.lastHeartbeat);
        }
        if (typeof this.lastOutput === "string") {
            this.lastOutput = new Date(<string><any>this.lastOutput);
        }
        if (this.deployment) {
            models.RecordIdentity.fixupPrototype(this.deployment);
        }
        if (this.imageReference) {
            models.RecordIdentity.fixupPrototype(this.imageReference);
        }
        if (this.mounts) {
            for (let key in this.mounts) {
                let val = this.mounts[key];
                if (val) {
                    models.Mountpoint.fixupPrototype(val);
                }
            }
        }
    }

    sysId: string;

    createdOn: Date;

    updatedOn: Date;

    lastHeartbeat: Date;

    deployment: models.RecordIdentity;

    dockerId: string;

    status: models.DeploymentStatus;

    image: string;

    imageReference: models.RecordIdentity;

    name: string;

    purpose: models.DeploymentRole;

    labels: { [key: string]: string; };

    mounts: { [key: string]: models.Mountpoint; };

    restartCount: number;

    lastOutput: Date;

    lastOffset: number;

}
